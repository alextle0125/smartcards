<h2>Sign up</h2>

<div class="registration">

  <%= form_for @user, url: { action: 'create'}, html: {class: 'registration-signup-form' } do |f| %>
  	<%= f.label :first_name, :class =>"form-label" %>
  	<%= f.text_field :first_name, :class => "form-input" %><br>
  	<%= f.label :last_name, :class =>"form-label" %>
  	<%= f.text_field :last_name, :class => "form-input" %><br>
  	<%= f.label :email, :class =>"form-label" %>
  	<%= f.email_field :email, :required => true, :class => "form-input" %><br>
  	<%= f.label :username, :class =>"form-label" %>
  	<%= f.text_field :username,  :required => true, :class => "form-input" %><br>
  	<%= f.label :password, :class =>"form-label" %>
  	<%= f.password_field :password,  :required => true, :class => "form-input" %><br>
  	<%= f.submit "sign up", :class => "registration-signup-btn" %>
  	<% if @error %>
    	<p class="registration-errors"><%= @error %></p>
  	<% end %>
  <% end %>

  <div class="registration-text-field">
    <h6>smartcards is a flashcard app. Join smartcards to contribute to the smartcard community and compete with other members. smartcards will keep track of your progress and map out your personal growth.</h6> <span>Register for a new smartcard account and start your learning.</span>
  </div>
  
  <%= link_to 'log in',  login_path, :class => "registration-login-btn" %>

</div>
